import { RestaurantAdmins } from './restaurant_admins.entity';

describe('RestaurantAdminsEntity', () => {
  it('should be defined', () => {
    expect(new RestaurantAdmins()).toBeDefined();
  });
});
